import React from 'react';

import {Container, StyledText} from './styles';

export const LoginScreen: React.FC = () => {
  return (
    <Container>
      <StyledText>Tela de Login</StyledText>
    </Container>
  );
};
